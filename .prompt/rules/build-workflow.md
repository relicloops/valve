---
name: build-workflow
description: Rules for when to build or test. Always ask before running build/test commands.
---

# Build Workflow

**ALWAYS ask before building or testing.** 

- Do not automatically run build or test commands after making changes.
- Exception: Only build/test without asking if the user explicitly requests it.
